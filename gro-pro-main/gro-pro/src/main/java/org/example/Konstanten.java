package org.example;

/**
 * Zentrale Projektkonstanten.
 * Änderungen hier gelten automatisch für alle Algorithmen.
 */
public final class Konstanten {

    private Konstanten() {}

    /** Haltezeit in Minuten, die ein Zug an jeder Zwischenstation verbringt (Ein-/Aussteigen). */
    public static final int HALTEZEIT = 1;

    /** Mindestabstand in Minuten, der nach einer Begegnung zwischen zwei Zügen eingehalten werden muss. */
    public static final int SICHERHEITSZEIT = 1;
}
